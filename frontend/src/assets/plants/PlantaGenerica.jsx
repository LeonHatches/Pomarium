import React from "react";
import { IconHoja } from "../icons";

/** Ícono de respaldo para cualquier especie sin ilustración propia todavía. */
export default function PlantaGenerica(props) {
  return <IconHoja {...props} />;
}
